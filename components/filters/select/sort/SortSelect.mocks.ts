import { ISortSelect } from './SortSelect';

const base: ISortSelect = {
  sortOptions: [
    'Newest to Oldest',
    'Oldest to Newest',
    'Price Ascending',
    'Price Descending',
  ],
};

export const mockFilterCategoryProps = {
  base,
};
